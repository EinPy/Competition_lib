//solution to https://codeforces.com/contest/1761/problem/D
#include <bits/stdc++.h>
#define all(x) begin(x),end(x)
#define srtarr(x) x, x + sizeof(x) / sizeof(x[0])

using namespace std;

using ll = long long;
using ull = unsigned long long;

void solve() {
    
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int n, k;
    cin >> n >> k;
    // number of ordered pairs a, b where f(x,y) == k
    //iterate over number, number of ones in binary
    //find number whery there are k 2 carries?
    //
}