#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

void solve() {
    
}

int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    double a;
    cin >> a;
    // a* x = 1
    // x = 1 / a
    cout << 100 / a << endl;
    cout << 100 / (100-a) << endl;
}